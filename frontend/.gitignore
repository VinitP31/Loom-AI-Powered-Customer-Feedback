# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# jsPDF's doc.save() falls back to writing to cwd under jsdom (no real
# browser download API in the test environment) — stray output from
# exportReport.test.ts, not a real build artifact.
loom-report.pdf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
